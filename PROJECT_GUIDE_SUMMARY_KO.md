# 미국 서부 2027 여행 프로젝트 요약 설명서

> 이 문서는 빠르게 구조와 운영 방법을 확인하기 위한 요약본이다.
> 더 자세한 원리, SQL 해설, 용어 설명은 [상세 설명서](./PROJECT_GUIDE_KO.md)를 참고한다.
> 현재 기준: 2026-08-21 · `schemaVersion 4`

---

## 1. 한 문장 요약

이 프로젝트는 **GitHub Pages가 `index.html` 웹앱을 배포하고, Supabase가 최신 일정·로그인·저장 버전·사진·실시간 동기화를 담당하는 여행 계획 웹앱**이다.

---

## 2. 전체 구조

```text
[프로그램 코드]
내 PC의 파일
    ↓ git commit / push
GitHub 저장소
    ↓ GitHub Pages 배포
사용자의 브라우저에서 index.html 실행

[여행 데이터]
사용자의 브라우저
    ↕ 읽기·저장·로그인·사진·변경 알림
Supabase
```

각 구성 요소를 쉽게 비유하면 다음과 같다.

| 구성 요소 | 쉬운 비유 | 역할 |
|---|---|---|
| `index.html` | 여행 안내판 본체 | 화면, 디자인, 버튼, 모든 JavaScript 기능 |
| GitHub | 안내판 설계도 보관함 | 코드와 문서의 변경 이력 보관 |
| GitHub Pages | 안내판 설치 서비스 | `index.html`을 공개 웹주소에 배포 |
| Supabase Database | 온라인 여행 노트 | 최신 일정과 최근 50개 저장본 보관 |
| Supabase Auth | 편집실 출입 확인 | 편집자 이메일과 비밀번호 확인 |
| Supabase Storage | 사진 창고 | 압축된 일정 사진과 썸네일 보관 |
| Supabase Realtime | 변경 알림 방송 | 저장된 새 내용을 다른 브라우저에 전달 |
| `supabase_setup.sql` | 서버 공사 지시서 | 표, 권한, 함수, 사진 공간, 실시간 설정 생성 |

---

## 3. 가장 중요한 구분

### 프로그램을 바꿀 때

다음은 `index.html` 같은 파일을 수정해야 한다.

- 디자인 변경
- 버튼 추가
- 동작 방식 변경
- 자동 로그아웃 시간 변경
- 새로운 기능 구현

반영 과정:

```text
파일 수정 → 확인 → Git commit → Git push → GitHub Pages 재배포
```

### 여행 내용을 바꿀 때

다음은 공개 웹페이지의 편집 기능을 사용한다.

- 일정 추가·수정
- 체크리스트 수정
- 가계부 입력·수정·순서 변경
- 환율 변경
- 사진·지도 링크·주의사항 추가

반영 과정:

```text
웹페이지에서 편집 → 저장 확인 → Supabase 저장 → 다른 방문자에게 반영
```

여행 내용만 수정했다면 일반적으로 Git commit이나 push는 필요 없다.

> Git push는 프로그램 코드를 배포하고, 웹페이지의 저장 버튼은 여행 데이터를 Supabase에 저장한다.

---

## 4. 파일별 역할

| 파일 또는 폴더 | 역할 | 직접 수정해도 되는가 |
|---|---|---|
| `index.html` | 웹앱 본체 | 기능과 디자인을 바꿀 때 수정 |
| `supabase_setup.sql` | Supabase 서버 설정 | 내용을 이해하고 SQL Editor에서 실행 |
| `README.md` | 짧은 운영 안내 | 필요하면 수정 가능 |
| `PROJECT_GUIDE_KO.md` | 주 상세 설명서 | 이 프로젝트를 공부할 때 먼저 참고 |
| `PROJECT_GUIDE_SUMMARY_KO.md` | 현재 요약 설명서 | 빠른 확인용 |
| `.gitattributes` | Windows/WSL 줄바꿈 통일 | 특별한 이유가 없으면 유지 |
| `.git/` | Git 내부 기록 | 직접 수정하거나 삭제하지 않음 |
| `assets/` | 숙소 사진·지도 캡처 WebP 정적 자산 | 이미지 교체 시 참조 경로도 함께 수정 |
| `test.html`, `test2.html` | 여행 진행 중·종료 후 헤더 미리보기 | 개발·디자인 확인용 |
| `reference.html` | UI·기능 비교용 참고 파일 | 실제 공개 페이지 실행에는 사용되지 않음 |
| `.agents/skills/` | 코딩 도구용 디자인 지침 | 실제 웹페이지 실행에는 사용되지 않음 |

---

## 5. 페이지를 열면 일어나는 일

1. GitHub Pages가 방문자에게 `index.html`을 전달한다.
2. 브라우저가 HTML 화면과 CSS 디자인을 표시한다.
3. 브라우저 안에서 JavaScript가 실행된다.
4. JavaScript가 Supabase의 최신 여행 저장본을 요청한다.
5. 저장본이 있으면 `index.html`의 기본 일정을 Supabase 내용으로 교체한다.
6. 최근 상태를 브라우저 `localStorage`에도 보조 캐시로 저장한다.
7. Supabase Realtime 변경 알림을 구독한다.
8. 서울·미국 서부 시각과 시차를 표시하고 1분마다 갱신한다.
9. 여행 진행률을 표시하고 한 시간마다 다시 계산한다.
10. 미국 서부 날짜를 기준으로 여행 전에는 PREP, 여행 중에는 해당 날짜, 여행 후에는 가계부를 첫 화면으로 선택한다.

Supabase 연결이 실패하면 브라우저의 최근 캐시를 사용하고, 캐시도 없으면 HTML 안의 기본 내용을 표시한다.

> `index.html`의 기본 일정을 직접 바꿔도 Supabase 최신 저장본이 있으면 페이지 로딩 후 다시 덮어쓸 수 있다.

---

## 6. 편집과 저장 흐름

### 편집 시작

1. `편집 시작`을 누른다.
2. 유효한 편집자 세션이 없으면 비밀번호를 입력한다.
3. Supabase Auth가 비밀번호를 확인한다.
4. 로그인에 성공하면 최신 공개본을 다시 읽는다.
5. 일정 제목, 시간, 설명 등을 편집할 수 있게 한다.

비밀번호는 HTML에 저장되어 있지 않으며 Supabase Auth가 확인한다.

### 편집 중

- 변경 내용은 우선 현재 브라우저에만 존재한다.
- 아직 다른 사람에게 보이지 않는다.
- 실행 취소와 다시 실행을 사용할 수 있다.
- 일정마다 사진·지도 링크·주의사항을 추가할 수 있고, 주의사항은 여러 개를 수정·삭제할 수 있다.
- PREP 체크리스트 항목을 추가·수정·삭제하고 위·아래 버튼으로 순서를 바꿀 수 있다.
- 새 체크 항목은 빈 값에 안내 문구만 보이므로 별도로 지우지 않고 바로 입력한다.
- 마지막 저장 상태와 현재 상태를 비교해 변경 여부를 판단한다.

### 편집 완료

변경이 없다면 바로 종료한다.

변경이 있으면 다음 중 하나를 선택한다.

- `저장하고 종료`: Supabase에 기록하고 공개한다.
- `계속 편집`: 편집을 이어 간다.
- `변경사항 폐기`: 마지막 저장본으로 되돌린다.

체크 완료 여부와 가계부 내역처럼 보기 모드에서 빠르게 바꾼 내용은 초안으로 기록된다. 이때만 `변경사항 저장` 버튼이 나타나며, 저장 전에는 다른 방문자에게 공개되지 않는다.

### 저장할 때

브라우저가 Supabase의 `save_trip_document` 함수를 호출한다.

Supabase는 다음 순서로 처리한다.

1. 로그인 사용자가 지정 편집자인지 확인한다.
2. 새 상태를 버전 표에 기록한다.
3. 현재 공개 문서를 새 상태로 교체한다.
4. 최근 50개를 넘는 오래된 버전을 삭제한다.
5. 다른 브라우저에 변경 알림을 보낸다.

---

## 7. 저장되는 데이터

Supabase에는 대략 다음과 같은 JSON 상태가 저장된다.

```json
{
  "schemaVersion": 4,
  "html": "일정과 체크리스트의 HTML",
  "expenses": "가계부 항목 배열",
  "categories": "가계부 카테고리 배열",
  "exchangeRate": 1450,
  "exchangeRateUpdated": "환율 수정일",
  "checks": [true, false, true]
}
```

- 일정과 체크리스트 구조는 HTML 문자열로 저장한다.
- 가계부와 카테고리는 계산하기 쉽도록 배열로 저장한다.
- 체크박스 선택 여부는 참/거짓 배열로 따로 저장한다.
- 체크리스트 순서는 HTML의 항목 순서로 저장한다.
- `schemaVersion`은 데이터 형식의 버전을 나타낸다.

---

## 8. 자동 로그아웃과 변경 보호

- 편집자 활동이 멈춘 뒤 29분이 지나면 경고가 표시된다.
- 30분이 되면 자동 로그아웃된다.
- 경고 후 1분 동안 응답이 없으면 저장하지 않은 초안을 폐기한다.
- 변경이 있으면 저장, 계속 사용, 폐기를 선택할 수 있다.
- `초기화`는 현재 초안을 마지막 저장본으로 되돌린다.
- 실행 취소/다시 실행 기록은 현재 브라우저 메모리의 임시 기록이다.
- 버전 관리는 Supabase에 보관되는 영구 저장 기록이다.

| 구분 | 실행 취소/다시 실행 | 버전 관리 |
|---|---|---|
| 위치 | 브라우저 메모리 | Supabase |
| 생성 시점 | 편집 중 | 저장 성공 시 |
| 최대 개수 | 100개 상태 | 최근 50개 저장본 |
| 과거 공개본 복원 | 불가 | 가능 |

---

## 9. 가계부

가계부 항목에는 다음 정보가 들어간다.

- 항목명
- 금액
- KRW 또는 USD
- 날짜
- 카테고리

주요 특징:

- 전체 일정 편집 모드를 켜지 않고도 가계부 입력 UI를 사용할 수 있다.
- 입력 영역은 가계부 위쪽에 있고 새 항목은 등록순 목록의 마지막에 추가된다.
- 기존 항목의 날짜, 카테고리, 항목명, 금액, 통화를 수정할 수 있다.
- 위·아래 버튼으로 등록 순서를 바꿀 수 있으며 이 순서도 저장 데이터에 포함된다.
- 실제 변경 시에는 유효한 편집자 인증이 필요하다.
- 변경 후 나타나는 `변경사항 저장`을 눌러야 다른 사람에게 반영된다.
- 가계부 탭에서는 공통 `편집 시작` 대신 `가계부 설정`이 표시된다.
- `가계부 설정`에서 수동 환율과 카테고리를 관리한다.
- 환율은 자동 시세가 아니라 사용자가 직접 입력하며 화면을 다시 그린 뒤에도 초안 값이 유지된다.
- USD 항목은 입력한 환율로 원화 환산 합계를 계산한다.
- 카테고리를 삭제하면 기존 지출은 `삭제된 카테고리`로 이동한다.
- 지출 기록 자체를 삭제할 때는 확인창을 표시한다.

---

## 10. 사진과 지도

### 사진

사진은 편집 화면에서 Supabase Storage로 올리는 동적 사진과, 저장소 `assets/`에서 GitHub Pages가 배포하는 숙소·지도 이미지로 나뉜다.

사진 추가 시 브라우저가 먼저 압축한다.

- 큰 보기용: 최대 변 1600px WebP
- 썸네일: 최대 변 480px WebP
- 선택 가능한 원본: 한 파일당 최대 20MB
- Storage 저장 파일: 한 파일당 최대 5MB

압축한 사진 내용의 해시를 파일명으로 사용한다.

```text
us-west-2027/photos/<해시>.webp
us-west-2027/thumbs/<해시>.webp
```

같은 사진은 중복 업로드를 줄일 수 있고, 사진 내용이 바뀌면 URL도 새로 생긴다. 브라우저는 바뀌지 않은 사진을 캐시에서 재사용할 수 있다.

화면에서 사진을 지워도 Storage 실제 파일은 바로 삭제하지 않는다. 과거 버전이 그 사진을 참조할 수 있기 때문이다.

### 지도

Google Maps 링크는 지도 데이터 자체를 저장하지 않는다.

- 장소명
- `https://` 외부 지도 링크

만 저장하므로 외부 지도를 열 때는 인터넷이 필요하다. 별도로 보이는 숙소 지도 캡처는 `assets/`의 비클릭 정적 이미지다.

---

## 11. 오프라인 작업본과 보기용 HTML

| 항목 | 오프라인 작업본 | 보기용 HTML |
|---|---|---|
| 목적 | 인터넷 없이 편집 | 읽기, 공유, 인쇄 |
| 편집 기능 | 있음 | 없음 |
| 사진 | HTML 안에 포함 | 주로 온라인 URL 사용 |
| Supabase 연결 | 없음 | 없음 |
| 온라인으로 다시 불러오기 | 가능 | 불가능 |
| 파일 크기 | 사진에 따라 큼 | 비교적 작음 |

### 오프라인 작업본

- 일정 전체 상태를 JSON으로 HTML 안에 넣는다.
- 사진을 Base64 문자열로 HTML 안에 포함한다.
- Supabase 연결 코드를 제거한다.
- 오프라인에서 수정 후 새 작업본 파일로 저장할 수 있다.
- 사진이 많으면 100MB 이상이 될 수 있다.

### 보기용 HTML

- 모든 편집 스크립트와 편집 도구를 제거한다.
- 가계부 입력·수정·삭제·순서 변경 UI를 제거한다.
- 체크박스와 입력 기능을 비활성화한다.
- 인쇄하거나 다른 사람에게 읽기 전용으로 전달할 때 사용한다.

### 작업본 불러오기

온라인 페이지에서 오프라인 작업본을 불러오면 다음을 확인한다.

1. 같은 여행에서 만든 작업본인가
2. 데이터 형식이 호환되는가
3. 위험한 스크립트나 HTML 속성이 없는가
4. 최신 Supabase 저장본과 무엇이 달라졌는가
5. 비교하는 동안 최신 저장본이 다시 바뀌지 않았는가

불러오기를 적용해도 먼저 초안이 되며, 다시 `저장`해야 공개된다.

---

## 12. Supabase SQL을 Run했다는 뜻

Supabase Dashboard의 SQL Editor에서 `supabase_setup.sql`을 붙여 넣고 `Run`했다는 것은 **인터넷의 Supabase PostgreSQL 서버에 설정 명령을 실행했다**는 뜻이다.

SQL은 다음을 만든다.

### `trip_documents`

현재 공개 여행 저장본 한 건을 보관한다.

- 여행 ID
- 전체 상태 JSON
- 편집자 계정 UUID
- 마지막 수정 시각

### `trip_document_versions`

저장할 때마다 전체 상태를 버전으로 보관한다. 최신 50개만 유지한다.

### 저장 함수

`save_trip_document` 함수가 다음을 한 번에 처리한다.

- 편집자 권한 검사
- 새 버전 생성
- 공개 최신본 업데이트
- 50개를 넘은 오래된 버전 삭제

### RLS 정책

- 최신 공개 일정은 누구나 읽을 수 있다.
- 저장은 지정 편집자만 할 수 있다.
- 버전 목록도 지정 편집자만 볼 수 있다.

### 사진 Storage

- `trip-media` 버킷을 만든다.
- WebP 파일만 허용한다.
- 공개 URL을 통한 사진 보기는 허용한다.
- 지정 편집자만 해당 여행 경로에 업로드할 수 있다.

### Realtime

`trip_documents`가 업데이트되었을 때 연결된 브라우저에 알릴 수 있게 한다.

> `supabase_setup.sql`을 GitHub에 push하는 것만으로 Supabase 설정이 실행되지는 않는다. 실제 변경은 SQL Editor에서 Run해야 한다.

---

## 13. 공개 키와 보안

`index.html`에 Supabase 프로젝트 URL, publishable key, 편집자 이메일이 보이는 것은 구조상 예상된 일이다. 브라우저로 전달되는 코드는 방문자가 볼 수 있기 때문이다.

publishable key 자체를 비밀번호로 사용하지 않는다. 실제 권한은 다음이 결정한다.

- Supabase 로그인 세션
- 데이터베이스 RLS
- Storage 정책
- `save_trip_document` 내부의 편집자 UUID 검사

절대로 HTML이나 GitHub에 넣으면 안 되는 것:

- 편집자 비밀번호
- Supabase secret 또는 `service_role` 키
- 데이터베이스 비밀번호
- 다른 서비스의 비밀 API 키

공개 일정과 공개 사진 URL은 누구나 볼 수 있으므로 예약번호 전체, 여권 정보, 결제 정보 등은 기록하지 않는 것이 안전하다.

---

## 14. 문제가 생겼을 때 빠른 확인표

### Push했는데 화면이 안 바뀜

1. 올바른 브랜치에 push했는지 확인
2. GitHub Pages 배포 성공 여부 확인
3. 몇 분 기다린 후 강력 새로고침
4. 일정 변경이었다면 Git push가 아니라 웹페이지 `저장`이 필요한지 확인

### HTML 기본 일정을 바꿨는데 옛 일정이 보임

Supabase 저장본이 페이지 로딩 후 HTML 기본 내용을 덮어썼을 가능성이 높다.

### 저장 실패

1. 편집자 로그인이 유효한지 확인
2. Supabase에 `us-west-2027` 문서 행이 있는지 확인
3. 문서의 `editor_id`와 로그인 계정 UUID가 같은지 확인
4. 최신 `save_trip_document` SQL이 실행되었는지 확인
5. 네트워크와 브라우저 오류 메시지 확인

### 사진 업로드 실패

1. 편집자 로그인 확인
2. 원본 20MB 제한 확인
3. `trip-media` 버킷 확인
4. 업로드 정책과 WebP 제한 확인
5. 네트워크 확인

### 다른 사람에게 즉시 안 보임

1. 편집자가 실제로 저장했는지 확인
2. 보는 사람이 편집 또는 버전 미리보기 중인지 확인
3. Realtime SQL 설정 확인
4. 새로고침했을 때 보이는지 확인

### 계속 비밀번호를 요구함

1. 30분 비활성 시간이 지났는지 확인
2. 브라우저가 사이트 저장 데이터를 차단하는지 확인
3. `file://` 로컬 파일과 `https://` 공개 주소를 번갈아 쓰는지 확인
4. 다른 브라우저나 시크릿 창인지 확인

### PowerShell과 WSL의 Git 결과가 다름

줄바꿈 차이를 의심한다. `.gitattributes`가 LF로 통일하도록 설정되어 있으므로 실제 내용 변경과 줄바꿈 변경을 구분한다.

---

## 15. 안전한 운영 체크리스트

### 코드를 바꾸기 전

- `git status`로 기존 변경 확인
- `git diff`로 실제 변경 내용 확인
- 민감한 키나 비밀번호가 들어가지 않았는지 확인

### 코드를 바꾼 후

- PC와 모바일 화면 확인
- 보기 모드와 편집 모드 확인
- 저장하지 않은 초안이 공개되지 않는지 확인
- 저장 후 다른 브라우저에서 보이는지 확인
- 가계부 합계와 환율 확인
- 사진, 버전 복원, 오프라인 작업본 확인

### SQL을 실행하기 전

- SQL 전체 내용을 읽기
- `drop table`, `delete`, `truncate` 같은 삭제 명령 확인
- 편집자 계정이 먼저 생성되었는지 확인
- 실행 결과와 오류 메시지 확인

### 하지 말아야 할 일

- `.git` 폴더 직접 수정 또는 삭제
- 비밀번호나 secret 키를 HTML에 저장
- 과거 버전의 참조를 확인하지 않고 사진 일괄 삭제
- SQL 파일을 push하면 서버에도 자동 적용된다고 생각하기
- 저장하지 않은 초안이 서버에 남아 있을 것이라고 기대하기

---

## 16. 최종 핵심 다섯 문장

1. `index.html`은 화면·디자인·JavaScript 기능을 모두 담은 웹앱 본체다.
2. GitHub Pages는 프로그램 코드를 배포하고 Supabase는 여행 데이터와 로그인을 담당한다.
3. Git push와 웹페이지 저장은 서로 다른 저장 경로다.
4. SQL Editor의 Run은 Supabase 서버에 표·함수·권한·사진 공간을 실제로 만드는 작업이다.
5. publishable key는 공개 사용을 전제로 하지만 비밀번호와 secret/service role 키는 절대로 공개하면 안 된다.

더 상세한 설명은 [PROJECT_GUIDE_KO.md](./PROJECT_GUIDE_KO.md)를 참고한다.
