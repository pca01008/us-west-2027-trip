{
  "name": "us-west-2027-trip",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "styles:consolidate": "node scripts/consolidate-styles.mjs",
    "test": "node --test tests/*.test.mjs && node scripts/check-project.mjs",
    "test:browser": "node scripts/check-browsers.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@supabase/supabase-js": "2.112.4",
    "playwright": "1.62.1"
  }
}
